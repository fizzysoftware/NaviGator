<div class="bars_background">
  <div class="managebarcontainer">
    <%#- model_class = User -%>
  
  <!-- <h1 class="userpage_title"><%#=t '.title', :default => model_class.model_name.human %></h1>

  <div class="user_profile_name">
      <strong><%#= model_class.human_attribute_name(:name) %>:</strong>
      <%= @user.name %>
  </div>
  <div class="user_profile_name">
      <%#= model_class.human_attribute_name(:email) %>:
      <%= @user.email %>
  </div>
  <hr> -->
  <%- model_class = User -%>
  <h1 class="userpage_title"><%=t '.title', :default => t('helpers.titles.edit', :model => model_class.model_name.human, :default => "Edit #{model_class.model_name.human}") %></h1>

  <%= render :partial => 'form' %>

  <!-- <div class="form-actions embeded_padding">
    <%#= link_to t('.back', :default => t("helpers.links.back")),
    users_path, :class => 'btn'  %>
    <%#= link_to t('.edit', :default => t("helpers.links.edit")),
    edit_user_path(@user), :class => 'btn btn-info' %>
    <%#= link_to t('.destroy ', :default => t("helpers.links.destroy")),
    user_path(@user),
    :method => 'delete',
    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
    :class => 'btn btn-danger' %>
  </div> --><!-- form-actions embeded_padding -->


</div>
</div>

